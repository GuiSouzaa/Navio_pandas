Personalizando para Outras Colunas
Se você precisar buscar outras informações além do ID_PRODUTO (como peso, preço, etc.), basta adicionar essas colunas ao merge:

python
Copiar código
dados_combinados = pd.merge(
    orders, 
    base_dados_ajustada[['ITEM CODE', 'ID_PRODUTO', 'PESO_CAIXA', 'PRECO_VENDA']], 
    on='ITEM CODE', 
    how='left'
)